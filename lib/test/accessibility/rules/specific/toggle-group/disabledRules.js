/**
 * Array of accessibility rule IDs to be disabled in both Jest and Storybook for the toggle group component.
 *
 */
export const disabledRules = [
  // Disable color contrast rule to prevent errors from having grayish helper text for disabled elements. 
  "color-contrast",
];
