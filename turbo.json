{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".dist/**", ".next/**"]
    },
    "dev": {
      "cache": false
    },
    "test": {
    },
    "test:watch": {
    }
  }
}
